version: '3.9'

services:
  experiment-runner:
    image: utgen-nlp-image   # <-- Use the name of your prebuilt image
    container_name: experiment-runner
    entrypoint: [ "bash", "/app/run-experiment.sh" ]
    command: [ "utgen", "3" ]  # <-- Replace with your actual args or override at runtime